#include "device.h"

class Device : NamedElement
{
	//std::vector<DeviceElementGroup> device_elements;
	/*Device(string refId, string name, string desc)
	{
		device_elements = new std::vector<DeviceElementGroup>();
		this->RefId = refId;
		this->Name = name;
		this->Desc = desc;
	}*/
};